# HANDOFF — 2026-05-19 (EOD)

## TL;DR

Dev DB password rotated (P0 cleared). PR #54 (patch date convention) merged to main. Three GitHub issues filed (#69, #70, #71) capturing 3c follow-up scope. Leaked Password Protection enabled in prod Supabase Auth. Tomorrow's primary work is **PR #34 rebase + finish step 3c (5/7 already on branch) + ship**.

## What shipped today

- **PR #54** (`fix/patch-date-convention`) — squash-merged to main. Independent fix, no follow-ups.
- **Dev DB password rotation** (project ref `dbgiinfiddvnbpwcagml`) — rotated in Supabase dashboard, local `.env` updated, connection verified via `psql "$DIRECT_URL" -c "SELECT 1;"`. Old leaked credential no longer valid.
- **Leaked Password Protection enabled** — prod Supabase Auth → Attack Protection → "Prevent use of leaked passwords" toggled ON. Security Advisor warning cleared.
- **GitHub issues filed** for 3c follow-ups (out of #34 scope by design):
  - #69 — Soft confirmation gate when removal date exceeds 30 days
  - #70 — Cross-check patch number between working-copy and executed CoCs
  - #71 — Add automated route-level tests for CoC upload paths
- **Memory cleanup:** deleted `~/.claude/projects/.../memory/truetest_dev_db_creds_rotation_owed.md` (rotation complete, file was stale).

## Tomorrow — Path A continuation for #34

Per memory triage and the 3c plan doc at `~/.claude/plans/2026-05-06-cap-1-3c-patch-executed-coc-upload.md`:

1. **Sync local main:** `git checkout main && git pull`
2. **Rebase `feat/patch-coc-upload` (PR #34) against main.** Branch is 10 commits behind with conflicts to resolve.
3. **Complete steps 6-7 of the 3c plan:**
   - Step 6: `npx tsc --noEmit && npm test` should pass clean (~15-60 min)
   - Step 7: 14-step DoD manual walkthrough — **run locally, NOT on Preview**. Half-day to full-day depending on fixture availability and first-pass success.
4. **Fixtures needed for step 7:** real WORN-state patch case (applicationDate + workingCopyDocumentId set, executedDocumentId null), PharmChek executed-copy PDF with box 10 filled, second working-copy regression case, non-patch test order for cross-regression.
5. **Merge #34** once DoD passes.
6. **Rebase + ship #60** (specimen ID persistence — conflicts with #34 on `documents/route.ts` + `CocConfirmModal.tsx`).
7. **Rebase + ship #68** (CoC date writes + ordering — depends on #34 being in main; also needs `paymentMethod` carried into PR #68's branch's `PatchOrderForUI` type per audit).
8. **End-to-end sweat patch entry from scratch.** Screenshot residual bugs.

## Step 3c status on #34's branch

3c is **5/7 done**, not 1/7 as the stale May 6 EOD comment on PR #34 implied. Commits `cdfb897` → `0c1cea7` (May 6-7) cover steps 1-5: extractor extension, modal props, button props, API branch in `documents/route.ts`, PatchSection wiring. Only steps 6 (tsc + tests) and 7 (manual DoD) remain.

The May 6 comment was stale by the next morning. Lesson: PR comments need updating or deletion when work moves past them.

## Corrections to fold into `truetest_infra.md`

1. **Vercel Preview env scope is Production-only**, not shared with Production. `DATABASE_URL` and `DIRECT_URL` are NOT in the Preview scope at all. The standing reminder "Preview connects to prod by default — loaded gun" is no longer accurate for this project. Preview deploys currently have no DB connection string at runtime. Step 7 must run locally because of this.
2. **Dev DB rotation P0 is resolved** as of today.

## Sparring observations parked for future sessions

- **`truetest-site` repo is in iCloud Drive** (`~/Library/Mobile Documents/com~apple~CloudDocs/playground/`). Working-tree files including `.git` are auto-replicated to Apple's servers and every Apple-ID-signed device. May affect May 18 Panel Advisor incident response retrospectively if Anthropic key was ever in working-tree files. Park until Tier 2 work (post-Denver move per 2026-05-19 chat).
- **Claude Code memory for at least one project is in iCloud Drive** (discovered via the deleted file path containing `Mobile-Documents-com-apple-CloudDocs`). Memory files often contain infrastructure context. Address as part of the iCloud cleanup pass.
- **Five sweat-patch PRs were sitting open simultaneously** (#34, #54, #60, #62, #68) until today. Each is "almost done"; together they form a conflict-compounding chain. Worth adopting: "no PR open more than 7 days without explicit triage (ship / close / commit to a date)."

## Standing security habits adopted today

- `read -rs "VAR?prompt"` instead of `export VAR=...` when handling credentials in shell. Prevents passwords landing in `~/.zsh_history`.
- Code repos do not live in iCloud Drive (currently violated by truetest-site; remediation deferred to Tier 2).
- When uncertain whether a credential was exposed, rotate. Cost of rotation < cost of uncertainty.
